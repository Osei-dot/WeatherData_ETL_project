# Core dependencies
apache-airflow==3.0.6

# For API requests (weather data extraction)
requests>=2.31.0

# For handling JSON / data processing
pandas>=1.5.3
numpy >=1.26.4

#Latest 1.4.x line, compatible with Airflow 3.0.6
SQLAlchemy >=1.4.54

#Stable Postgres driver for Python 3.12
psycopg2-binary	2.9.9


# Optional: Visualization of DAGs (needs graphviz system package too)
graphviz>=0.20.3

# Optional: If using Kubernetes executor
apache-airflow-providers-cncf-kubernetes

